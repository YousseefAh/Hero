import PayCreatePage from "@/components/pages/PayCreatePage";

export const metadata = {
  title: "إنشاء رابط دفع — BePrime",
  robots: "noindex, nofollow",
};

export default function Page() {
  return <PayCreatePage />;
}
