import DashboardPage from "@/components/pages/DashboardPage";

export const metadata = {
  title: "Dashboard — BePrime",
  robots: "noindex, nofollow",
};

export default function Page() {
  return <DashboardPage />;
}
